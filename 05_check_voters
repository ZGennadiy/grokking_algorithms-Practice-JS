const voted = {};
const hasVoted = (name) => {
  if (voted[name]) {
    console.log('kick them out!');
  } else {
    voted[name] = true;
    console.log('let them vote!');
  }
};


hasVoted('Vlad'); // let them vote!
hasVoted('Dmitriy'); // let them vote!
hasVoted('Dmitriy'); // kick them out!
